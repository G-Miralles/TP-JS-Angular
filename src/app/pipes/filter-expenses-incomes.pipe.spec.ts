import { FilterExpensesIncomesPipe } from './filter-expenses-incomes.pipe';

describe('FilterExpensesIncomesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExpensesIncomesPipe();
    expect(pipe).toBeTruthy();
  });
});
